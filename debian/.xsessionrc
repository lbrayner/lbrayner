[ -f ~/.profile ] && source ~/.profile

[ -x ~/.local/share/wm/scripts/login ] && ~/.local/share/wm/scripts/login

[ -x "$(command -v mpd)" ] && [ ! -s ~/.config/mpd/pid ] && mpd
[ -x "$(command -v mpdscribble)" ] && \
    [ -s ~/.config/mpd/pid ] && [ ! -s ~/.mpdscribble/pid ] && mpdscribble

[ -f ~/.xsessionrc.local ] && source ~/.xsessionrc.local

# vim: ft=sh
