. ~/.profile

~/.local/share/wm/scripts/login

[ ! -s ~/.config/mpd/pid ] && mpd
[ -s ~/.config/mpd/pid ] && [ ! -s ~/.mpdscribble/pid ] && mpdscribble

[ -f ~/.xsessionrc.local ] && ~/.xsessionrc.local

# vim: ft=sh
